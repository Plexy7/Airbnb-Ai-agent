# Airbnb-Ai-agent
The project uses pydantic ai as a agent with vllm as the brains of the model with MCP(Model context protocol) which is the bridge between the agent and api which let's the agent have the proper tools to execute the command.
